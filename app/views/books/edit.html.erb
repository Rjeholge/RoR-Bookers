<% flash[:notice] %>

<h1>Editing Book</h1>
<div id="error-msg">
<% if @book.errors.any? %>
 <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
   <% @book.errors.full_messages.each do |message| %>
    <li><%= message %></li>
   <% end %>
  </ul>
 <% end %>
 </div>

<%= form_with model: @book, url: book_path(@book.id), mehod: :update do |f| %>

 <div class="field">
  <label for="field-name">Title</label>
  <%= f.text_field :title %>
 </div>

 <div class="field">
  <label for="field-name">Body</label>
  <%= f.text_area :body %>
 </div>

 <div class="actions">
  <%= f.submit 'Update Book' %>
 </div>

<% end %>

 <%= link_to "Show", book_path(@book.id) %>
  |
<a href="/books">Back</a>